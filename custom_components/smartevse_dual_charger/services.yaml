refresh:
  name: Refresh controller
  description: Trigger an immediate control cycle.
  fields:
    entry_id:
      required: false
      example: "abcd1234"
      selector:
        text:

reset_sessions:
  name: Reset charge cycle
  description: Clear the active SmartEVSE selection so the current charge policy restarts immediately.
  fields:
    entry_id:
      required: false
      example: "abcd1234"
      selector:
        text:
